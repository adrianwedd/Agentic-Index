"""Utility helper functions used across AgentOps scripts."""

def add(a: int, b: int) -> int:
    """Return the sum of two integers.

    Args:
        a: First operand.
        b: Second operand.

    Returns:
        The integer sum of ``a`` and ``b``.
    """

    return a + b
