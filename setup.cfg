[metadata]
name = agentic-index
version = 0.1.0
description = CLI for Agentic Index

[options]
packages = find:
python_requires = >=3.8
install_requires =
    requests
    rich
    typer[all]

[options.entry_points]
console_scripts =
    agentic-index = agentic_index_cli.__main__:main
