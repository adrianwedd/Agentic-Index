[metadata]
name = agentops
version = 0.1.0
description = CLI for AgentOps

[options]
packages = find:
python_requires = >=3.8
install_requires =
    requests

[options.entry_points]
console_scripts =
    agentops = agentops_cli.__main__:main
